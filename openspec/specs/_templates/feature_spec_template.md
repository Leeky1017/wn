# Spec: <spec-id>

## Purpose

<One paragraph: what outcome we want.>

## Requirements

### Requirement: <clear requirement statement>

The system MUST ...

#### Scenario: <happy path>
- **WHEN** ...
- **THEN** ...

### Requirement: <error handling>

The system MUST return a stable structured error (`error_code` + `message`).

#### Scenario: <error path>
- **WHEN** ...
- **THEN** ...
